# 🤖 БЫСТРАЯ НАСТРОЙКА ИНТЕГРАЦИИ БОТА

## ✅ ЧТО БЫЛО СДЕЛАНО

Ваш сервер обновлен до версии **v5.2** с полной поддержкой интеграции бота!

### Добавлено:
- ✅ **POST /api/message_sent** - прием данных о сообщениях от бота
- ✅ **POST /api/heartbeat** - мониторинг статуса бота каждые 30 секунд
- ✅ **POST /api/error** - централизованное логирование ошибок бота
- ✅ Автоматическое создание таблицы `heartbeats` в БД
- ✅ Тестовые скрипты для проверки работоспособности
- ✅ Подробная документация

---

## 🚀 ПОШАГОВАЯ ИНСТРУКЦИЯ

### Шаг 1: Установите недостающие зависимости

```bash
cd /home/user/Server
npm install
```

Это установит `axios` для тестового скрипта.

---

### Шаг 2: Добавьте тестовую анкету в базу данных

Подключитесь к PostgreSQL и выполните:

```bash
psql -U postgres -d ladabot_stats
```

Затем выполните SQL:

```sql
-- Добавьте тестовую анкету
INSERT INTO allowed_profiles (profile_id, note)
VALUES ('TestProfile001', 'Тестовая анкета для бота')
ON CONFLICT (profile_id) DO NOTHING;

-- Проверьте, что анкета добавлена
SELECT * FROM allowed_profiles WHERE profile_id = 'TestProfile001';

-- Выход
\q
```

---

### Шаг 3: Запустите сервер

```bash
npm start
# или
node server.js
```

Вы должны увидеть:

```
⚙️ Проверка таблиц базы данных v5.2 (с интеграцией бота)...
✅ База данных готова к работе (v5.2 - с поддержкой бота)
🚀 CRM System v5.2 (Полная аналитика + Интеграция бота) запущен на порту 3000

📡 Эндпоинты для бота:
   • POST /api/message_sent - отправка данных о сообщениях
   • POST /api/heartbeat - heartbeat каждые 30 секунд
   • POST /api/error - логирование ошибок бота
```

---

### Шаг 4: Протестируйте интеграцию

Откройте новый терминал и запустите тесты:

```bash
cd /home/user/Server
npm test
# или
node test_bot_endpoints.js
```

Вы должны увидеть результаты всех тестов:

```
🧪 НАЧИНАЕМ ТЕСТИРОВАНИЕ ЭНДПОИНТОВ БОТА...

============================================================
📨 ТЕСТ 1: POST /api/message_sent
============================================================
✅ УСПЕХ: {
  "status": "ok",
  "contentId": 123
}

============================================================
❤️  ТЕСТ 3: POST /api/heartbeat
============================================================
✅ УСПЕХ: {
  "status": "ok"
}

...

✅ ВСЕ ТЕСТЫ ЗАВЕРШЕНЫ!
```

---

## 📡 КАК НАСТРОИТЬ БОТА

### Конфигурация бота

Ваш бот должен отправлять данные на следующие URL:

```
http://188.137.253.169:3000/api/message_sent
http://188.137.253.169:3000/api/heartbeat
http://188.137.253.169:3000/api/error
```

### Пример кода для бота (JavaScript/Node.js)

```javascript
const axios = require('axios');

const SERVER_URL = 'http://188.137.253.169:3000';

// Отправка сообщения
async function sendMessageToServer(messageData) {
    try {
        const response = await axios.post(`${SERVER_URL}/api/message_sent`, {
            botId: "bot_12345",
            accountDisplayId: "Maria25",
            recipientId: "67890",
            type: "outgoing",
            length: messageData.text.length,
            isFirst: false,
            isLast: false,
            convId: `conv_${messageData.conversationId}`,
            responseTime: "00:02:30",
            status: "success",
            textContent: messageData.text,
            mediaUrl: null,
            fileName: null,
            translatorId: 1,
            errorReason: null
        });

        console.log('✅ Сообщение отправлено на сервер:', response.data);
    } catch (error) {
        console.error('❌ Ошибка отправки на сервер:', error.message);
    }
}

// Heartbeat каждые 30 секунд
setInterval(async () => {
    try {
        await axios.post(`${SERVER_URL}/api/heartbeat`, {
            botId: "bot_12345",
            accountDisplayId: "Maria25",
            status: "online",
            timestamp: new Date().toISOString(),
            ip: "127.0.0.1",
            systemInfo: {
                version: "1.0",
                platform: process.platform
            }
        });
        console.log('❤️ Heartbeat отправлен');
    } catch (error) {
        console.error('❌ Ошибка heartbeat:', error.message);
    }
}, 30000);

// Логирование ошибок
async function logError(errorInfo) {
    try {
        await axios.post(`${SERVER_URL}/api/error`, {
            botId: "bot_12345",
            accountDisplayId: "Maria25",
            endpoint: errorInfo.endpoint,
            errorType: errorInfo.type,
            message: errorInfo.message,
            rawData: errorInfo.data,
            userId: null
        });
    } catch (error) {
        console.error('❌ Не удалось залогировать ошибку:', error.message);
    }
}
```

---

## 📊 ВАЖНО: Добавление анкет бота

**ПЕРЕД запуском бота** добавьте все анкеты, с которых будет работать бот, в базу данных:

```sql
INSERT INTO allowed_profiles (profile_id, note)
VALUES
    ('Maria25', 'Анкета Марии'),
    ('Anna28', 'Анкета Анны'),
    ('Elena30', 'Анкета Елены')
ON CONFLICT (profile_id) DO NOTHING;
```

Без этого сервер будет игнорировать сообщения со статусом `"ignored"`.

---

## 📖 ДОКУМЕНТАЦИЯ

Полная документация находится в файле:
- **BOT_INTEGRATION_GUIDE.md** - подробное руководство по всем эндпоинтам

---

## 🔍 МОНИТОРИНГ

### Проверка последних сообщений от бота

```sql
SELECT
    m.timestamp,
    m.bot_id,
    m.account_id,
    m.type,
    m.status,
    mc.text_content
FROM messages m
LEFT JOIN message_content mc ON m.message_content_id = mc.id
ORDER BY m.timestamp DESC
LIMIT 20;
```

### Проверка heartbeats

```sql
SELECT * FROM heartbeats
ORDER BY created_at DESC
LIMIT 10;
```

### Проверка ошибок

```sql
SELECT * FROM error_logs
ORDER BY timestamp DESC
LIMIT 10;
```

---

## ⚙️ ПОЛЕЗНЫЕ КОМАНДЫ

```bash
# Запуск сервера
npm start

# Запуск тестов
npm test

# Миграция БД (создание таблицы heartbeats)
npm run migrate

# Просмотр логов (если используется PM2)
pm2 logs nova

# Перезапуск сервера (PM2)
pm2 restart nova
```

---

## 🐛 РЕШЕНИЕ ПРОБЛЕМ

### Проблема: "Анкета не найдена - игнорируем"

**Решение:** Добавьте анкету в таблицу `allowed_profiles`:

```sql
INSERT INTO allowed_profiles (profile_id, note)
VALUES ('ИмяАнкеты', 'Описание');
```

### Проблема: "Database connection failed"

**Решение:** Проверьте настройки подключения к PostgreSQL в `server.js`:

```javascript
const pool = new Pool({
    user: 'postgres',
    host: 'localhost',
    database: 'ladabot_stats',
    password: 'mikmik98',  // Проверьте пароль
    port: 5432,
});
```

### Проблема: "Cannot connect to server"

**Решение:**
1. Убедитесь, что сервер запущен: `ps aux | grep node`
2. Проверьте порт: `netstat -tulpn | grep 3000`
3. Проверьте firewall: `sudo ufw status`

---

## 📞 СЛЕДУЮЩИЕ ШАГИ

1. ✅ Добавьте все анкеты бота в `allowed_profiles`
2. ✅ Укажите путь к серверу в настройках бота: `http://188.137.253.169:3000`
3. ✅ Запустите бота
4. ✅ Проверьте, что данные поступают на сервер
5. ✅ Мониторьте статистику через dashboard

---

**Удачи с интеграцией! 🚀**
